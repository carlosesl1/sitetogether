name: Runner diagnostic

on:
  push:
    paths:
      - ".github/workflows/runner-diagnostic.yml"

permissions:
  contents: read

jobs:
  ubuntu-24:
    name: Ubuntu 24 runner
    runs-on: ubuntu-24.04
    steps:
      - name: Runner started
        run: echo "ubuntu 24 runner started"

  ubuntu-26:
    name: Ubuntu 26 runner
    runs-on: ubuntu-26.04
    steps:
      - name: Runner started
        run: echo "ubuntu 26 runner started"

  windows:
    name: Windows runner
    runs-on: windows-latest
    steps:
      - name: Runner started
        run: echo "windows runner started"
